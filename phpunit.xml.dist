<?xml version="1.0" encoding="UTF-8"?>
<phpunit bootstrap="vendor/autoload.php" colors="true">
  <testsuites>
    <testsuite name="My Test Suite">
      <directory>tests</directory>
    </testsuite>
  </testsuites>

  <!-- Code coverage configuration (example; adjust as needed) -->
  <coverage>
    <!-- In PHPUnit 10, code coverage filters are configured differently -->
    <!-- See the PHPUnit 10 docs for details -->
  </coverage>

  <!-- Logging configuration -->
  <logging>
    <junit outputFile="build/logs/junit.xml"/>
  </logging>
</phpunit>
